# Git Branches

A branch in Git is a separate line of development that allows developers to work on new features or fixes without affecting the main branch.

Branches are useful because they:
- allow safe experimentation
- let multiple developers work independently
- keep the main code stable while new features are developed